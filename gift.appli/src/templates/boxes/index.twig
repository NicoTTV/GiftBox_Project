{% extends "base.twig" %}

{% block main %}
    <ul>
    {% for box in boxes %}
        <li><a href="{{ url_for('boxId',{'id' : box.id})}}">{{ box.libelle }}</a></li>
    {% endfor %}
        <a href="{{ url_for('boxCreate') }}">Creez une nouvelle box ici !</a>
    </ul>
{% endblock %}

